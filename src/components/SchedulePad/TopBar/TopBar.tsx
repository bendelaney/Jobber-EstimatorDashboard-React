import styles from "./TopBar.module.scss";

const TopBar = () => {
  return (
    <div className={styles.topBar}>
      This is where the following buttons will go:
        - Sidebar toggle
        - Split view toggle
        - Sync to Jobber button
        - ??
    </div>
  );
};

export default TopBar;
